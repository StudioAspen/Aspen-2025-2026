using CharonsCorner.LevelEditor;
using NUnit.Framework;
using UnityEngine;

public class PathTexture : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    [SerializeField] SplinePath RebuildMesh;
    [SerializeField] SplinePath spline;

    [SerializeField] SplinePath mesh;
    [SerializeField] SplinePath pathMeshFilter;

    private void UpdateMesh()
    {
       

  
    }
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
